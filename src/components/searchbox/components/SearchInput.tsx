const SearchInput = () => {
	return (
		<input
			type="text"
			className="peer w-full h-14 pr-8 pl-5 drop-shadow rounded-3xl text-black focus:drop-shadow-none focus:outline-1 focus:outline-primaryLight"
			placeholder="Search anything..."
		/>
	);
};

export default SearchInput;
